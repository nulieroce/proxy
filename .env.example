# 🔧 CONFIGURACIÓN PRINCIPAL
# URL del backend que se va a proxificar (REQUERIDO)
TARGET_URL=https://mi-aplicacion-original.com

# Puerto del servicio (Render lo asigna automáticamente)
PORT=10000

# Entorno de ejecución
NODE_ENV=production

# 🛡️ RATE LIMITING
# Ventana de tiempo en milisegundos (15 minutos = 900000ms)
RATE_LIMIT_WINDOW_MS=900000

# Máximo número de peticiones por IP por ventana
RATE_LIMIT_MAX_REQUESTS=100

# 💚 KEEP-ALIVE (Para evitar sleep en plan gratuito de Render)
# URL del propio servicio para auto-ping (se configura después del deploy)
KEEP_ALIVE_URL=https://tu-proxy.onrender.com

# Intervalo de keep-alive en formato cron (cada 10 minutos)
KEEP_ALIVE_INTERVAL=*/10 * * * *

# Timeout para keep-alive requests en milisegundos
KEEP_ALIVE_TIMEOUT=10000

# Habilitar logging verbose para keep-alive (true/false)
KEEP_ALIVE_VERBOSE=false

# 🔍 DEBUGGING Y MONITOREO
# Habilitar logs detallados en desarrollo
DEBUG=proxy:*

# URL para health checks externos (opcional)
HEALTH_CHECK_URL=https://tu-proxy.onrender.com

# 🔒 CONFIGURACIONES DE SEGURIDAD ADICIONALES (Opcionales)
# Headers personalizados para agregar al proxy
# CUSTOM_HEADER_NAME=valor
# CUSTOM_HEADER_API_KEY=tu-api-key

# Configuración de CORS (opcional)
# CORS_ORIGIN=https://tu-frontend.com
# CORS_CREDENTIALS=true

# 📊 CONFIGURACIONES AVANZADAS (Opcionales)
# Timeout general para peticiones al backend (ms)
PROXY_TIMEOUT=30000

# Número máximo de redirects a seguir
PROXY_MAX_REDIRECTS=5

# User Agent personalizado para el proxy
PROXY_USER_AGENT=ProxyRender-Liberia/1.0

# ⚙️ CONFIGURACIONES DE RENDER (Solo para referencia)
# Estas son configuradas automáticamente por Render
# RENDER_EXTERNAL_URL=https://tu-servicio.onrender.com
# RENDER_SERVICE_NAME=proxyrender-liberia
# RENDER_GIT_COMMIT=abc123